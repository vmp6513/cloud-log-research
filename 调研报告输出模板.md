## O365

### 1. 日志分类

有哪些类别的日志，对每个日志类别进行简要描述。

<!--> 
举例：
在 O365 环境中，日志由 Audit logs, Sign-in logs, Provisioning logs 三部分组成：

| 日志类别 | 来源 API | 简要描述与安全意义 |
...

参考文档：xxx
<-->

---

### 2. 能力边界

描述日志提供的关键能力，边界在哪，有什么其他限制？

**注意**：你需要根据不同日志类别进行分类讨论。

#### 2.1 视角边界

<!-->
举例：
控制面可见，数据面受限：能清晰看到资源的配置变更（如：谁开启了外部分享权限、谁修改了 MFA 策略），但无法获取任何网络包（PCAP）或流日志（NetFlow），也无法获取终端内部的进程信息。

参考文档：xxx
<-->

#### 2.2 内容边界

<!-->
举例：
只记录动作，不记录载体：日志记录了 `FileDownloaded`（文件已下载），并提供文件名和路径，但它不会保存被下载文件的副本。

参考文档：xxx
<-->

#### 2.3 其他限制

请根据日志特点，结合普通用户容易混淆或存在理解偏差的点，梳理云日志的其他限制。

---

### 3. 日志采集流程

如果云厂商提供多套采集方案，你需要按以下的结构分别说明，然后基于实际拉取日志的场景进行方案对比。

#### 3.1 概要流程

<!-->
举例：
基于 Graph API 可以获取到 Audit log，需要用户提供 xxx 权限，然后调用 xxx API...

参考文档
<-->

#### 3.2 核心API

#### 3.3 返回格式

<!-->
举例：
以 json 格式返回，每个 json 是一个完整的日志...
<-->

#### 3.4 关键字段

<!-->
举例：
CreateTime：创建时间，说明了操作发生的时间，是进行告警研判的关键数据
<-->

#### 3.5 所需权限

#### 3.6 安全厂商使用策略

描述安全厂商如何基于此方案进行日志采集，有哪些值得关注的经验项，有哪些踩过的坑可以规避...

---

### 4. 日志示例

提供常见类型的日志示例，以及如何构造这种日志，以便能够直观认识。

**注意**：至少提供3种以上类型的日志示例和对应的构造方法。

---

### 5. 影响基于日志进行告警研判的关键特性

我需要将日志发送到上游的分析引擎进行告警研判，该场景下有哪些影响研判的关键特性？

#### 5.1 日志规模与客户等级的关联

日志规模决定了采集方案的设计，也会对上层告警研判和其他分析引擎有影响，需要根据实际情况分类讨论...

<!-->
举例：
日志量的产生并非线性增长，而是受业务模式驱动。我们需要分层讨论：

| 客户规模 | 预计日志量 | 关键特性与研判挑战 | 可行的应对方案 |
...

参考文档：xxx
<-->

#### 5.2 日志延迟

日志延迟决定了上层生成告警的时效性，对于不同客户需求/业务场景，不同的采集方案带来的延迟也不一样，需要分情况讨论...

<!-->
举例：
使用 Graph API 一般存在5-10分钟的延迟，整体实时较高。
意味着上层研判引擎应具备 xxx 的能力
从 xxx 案例来看，使用 Graph API的场景一般是 xxx
...

参考文档：xxx
<-->

#### 5.3 是否支持流式拉取

影响采集方案制定，也影响告警研判，要准确生成一个告警，可能需要联立时间跨度较大的日志，流式拉取的方案会影响整个采集方案的性能...


#### 5.4 是否支持API过滤

决定了是否可以在数据源侧就对日志进行初步筛选，避免全量拉取后在业务侧筛选，减少上层引擎负担...

#### 5.5 限流策略

决定了日志吞吐量，间接影响上层研判的时效性、准确性...

#### 5.6 其他关键特性

---

### 6. 安全厂商经验项总结

开放性内容，请根据以往的安全事故回溯，安全厂商提供的安全方案等多方资料，围绕云日志总结一下启发性的经验项。

<!-->
举例：
2020，AWS出现了数据泄露事件，经回溯，攻击者通过 xxx 方式进行了提权操作，进而攻陷了数据库。
对应时间段的审计日志记录了攻击者执行了登录 - 授权等操作，但这些操作无法和正常的操作进行区分，也没办法提供攻击者提权后在数据面的操作。
这就意味着，仅靠云日志无法提供实时的攻击检测，必须依靠端上的安全组件进一步进行组合关联分析...
<-->